{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://uau9.github.io/orange-geography-coach/schemas/learning-projects.v0.8.schema.json",
  "title": "橙子地理教练学习项目清单 v0.8",
  "type": "object",
  "required": ["schema_version", "projects"],
  "properties": {
    "schema_version": { "const": "0.8.0" },
    "projects": {
      "type": "array",
      "minItems": 6,
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["id", "eyebrow", "title", "summary", "action", "route", "cta", "status_kind", "accent", "order"],
        "properties": {
          "id": { "type": "string", "minLength": 1 },
          "eyebrow": { "type": "string", "minLength": 1 },
          "title": { "type": "string", "minLength": 1 },
          "summary": { "type": "string", "minLength": 1 },
          "action": { "enum": ["start-earth-motion", "start-solar-season", "start-solar-path", "start-time-lab", "start-next", "goto"] },
          "route": { "type": ["string", "null"] },
          "cta": { "type": "string", "minLength": 1 },
          "status_kind": { "enum": ["earth_motion", "solar_season", "solar_path", "time_lab", "diagnostic", "retest"] },
          "accent": { "enum": ["navy", "gold", "sky", "teal", "orange", "slate"] },
          "order": { "type": "integer", "minimum": 0 }
        }
      }
    }
  }
}
