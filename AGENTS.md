# 橙子地理教练：Codex 项目规则

## 项目目标

为 2027 年 6 月高考、选科语数英政史地的橙子，建立一个手机端优先的地理诊断与复习工具。首要目标不是题量，而是让每一次错误都转化为：

1. 学生留下真实的选择理由；
2. AI 提出可检验的错因假设和追问；
3. 家长确认、修正或标记“需要专业教师复核”；
4. 系统安排同类题复测，并记录是否真正掌握。

## 教育规则

- 学生没有作答前，不直接展示答案或完整解析。
- AI 的诊断必须标明“候选”或“已确认”，不能把猜测写成事实。
- 每道题必须有唯一编号、知识点、正确答案、错误选项对应的 `error_tag` 和可追溯解释。
- 原创题优先；引用教材、真题或外部资料时，记录来源并由家长/教师审核后再发布。
- 不把“看过解析”当成“掌握”。掌握必须通过延迟复测或学生口头/书面解释验证。
- 不做黑箱推荐。推荐下一题时说明依据：错因、前置知识、复测到期或连续正确。
- 不将 API 密钥写入前端、数据文件或 Git 历史。

## 工程规则

- 手机端优先，首版保持无后端、可离线使用；AI 先通过“复制诊断提示词→粘贴结果”接入。
- 数据与界面分离：题目放在 `data/`，结构约束放在 `schemas/`。
- 修改行为时同步更新 `docs/`，并运行 `npm test`。
- 版本化 schema，不静默破坏旧记录；导入数据前先校验版本。
- 不引入大型依赖，除非先说明为什么原生实现不够。

## 完成标准

完成一个功能前，必须能说明：学生如何使用、家长如何介入、数据如何保存、错误如何复盘，以及如何验证该功能没有把学生变成只看答案的人。
